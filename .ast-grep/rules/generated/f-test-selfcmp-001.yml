# GENERATED by tools/wall/quality.py from FAILURE_PATTERNS.md (F-TEST-SELFCMP-001).
# Do not edit: change the entry's ```ast-grep block and run
#   python3 tools/wall/quality.py rules
id: f-test-selfcmp-001
severity: warning
message: "F-TEST-SELFCMP-001 - an assertion whose expected side is derived from the actual side"
metadata:
  failure_class: F-TEST-SELFCMP-001
  source: "FAILURE_PATTERNS.md"
language: python
rule:
  any:
    - pattern: assert $X == $X
    - pattern: assert $X == dict($X, $$$)
    - pattern: $T.assertEqual($X, $X)
constraints:
  X:
    not:
      kind: call
note: The expected side is derived from the actual side, so this can never fail. State the expected value independently. A repeated CALL compared with itself is a determinism check and is deliberately not matched.
